@{
    ViewData["Title"] = "Course Index";
}

@model List<Course>


@foreach (var item in Model)
{
    @if (item.isActive)
    {
        @await Html.PartialAsync("_CourseCard", item);<!--PartialView e parametre olarak item degiskeni gonderildi-->
    }
    
}